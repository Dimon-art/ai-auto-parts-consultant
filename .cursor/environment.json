{
  "name": "AI Auto Parts Consultant",
  "user": "ubuntu",
  "install": "sudo apt-get update && sudo apt-get install -y python3.12-venv && python3 -m venv .venv && .venv/bin/pip install --upgrade pip && .venv/bin/pip install -r requirements.txt",
  "start": ".venv/bin/python init_dev_db.py",
  "terminals": [
    {
      "name": "web",
      "command": ".venv/bin/uvicorn web_app:app --host 0.0.0.0 --port 8023"
    }
  ],
  "ports": [8023]
}
